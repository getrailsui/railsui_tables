# frozen_string_literal: true

# The oldest Rails the gemspec supports (>= 7.2). The main Gemfile resolves to
# whatever is newest, so this pins the floor and lets CI prove it.
source "https://rubygems.org"

gem "rails", "~> 7.2.0"

gemspec path: ".."
